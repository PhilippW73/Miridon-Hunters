{{> layout/header}}
<div class="row">
    <div class="col-md-offset-1 col-md-4">
        {{> characters/character-battle-block Player}}
    </div>
    <div class="col-md-offset-2 col-md-4">
        {{> characters/character-battle-block Enemy}}
    </div>
        {{#each Stats}}
            Test: {{../Player.character_name}}
        {{!-- each stat, send in  --}}
            
        {{/each}}
</div>
<div class="row">
    <div class="col-md-12">
        <h3>Actions</h3>
        {{> battle/action}}
        <div id="comments">
            <p>
                Choose your actions for the round (one of each).
            </p>
        </div>
    </div>
</div>